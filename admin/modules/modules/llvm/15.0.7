#%Module 1.0

conflict llvm

set llvm_root /usr/local/llvm-15.0.7

# prepend because LLVM is often installed in /usr/bin/... as well
prepend-path PATH ${llvm_root}/bin
append-path CMAKE_PREFIX_PATH ${llvm_root}
